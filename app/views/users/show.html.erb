<% if current_user.id == @user.id %>
<h1>Your profil</h1>
<% else %>
<h1><%=@user.firstname%>'s profil'</h1><br>
<%end%>
    <div class="card-box">
      <div class="card border-dark mb-3" style="max-width: 18rem;">
        <div class="card-header"> User n°<%=@user.id%></a></div>
        <div class="card-body">
          <h5 class="card-title"><%= @user.firstname  %> <%= @user.lastname %></h5>
          <p class="card-text"><%= @user.email %></p>
        </div>
      </div>
    </div>
    <% if current_user.id == @user.id %>
    <p><a href="<%= edit_user_path(@user.id)%>" class="btn btn-primary">Edit</a><p>
    <% else %>
    <% end %>
